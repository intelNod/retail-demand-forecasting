# Defense Question Bank

Use this answer pattern: **direct answer -> exact evidence -> limitation or next step**. Never guess; if evidence cannot be found, say what must be verified.

| # | Likely defense question | Short answer | Exact evidence reference | Limitation or follow-up |
|---|---|---|---|---|
| 1 | What real problem does the project solve and who uses the output? | It forecasts next-week unit demand for one FOODS product in one store so a retail planner can review stock needs. It supports a decision and does not order automatically. | `PROJECT_BRIEF.md` sections 2-3 and 5; `RESPONSIBLE_AI.md` section "Intended use" | It does not optimize inventory costs or replace the planner. |
| 2 | Why is the dataset suitable, and what is its biggest risk? | M5 provides daily unit sales, prices, calendar events, holidays, and SNAP indicators across stores, which supports supervised demand forecasting. The largest transfer risk is that it represents 2011-2016 US Walmart data. | `PROJECT_BRIEF.md` section 4; `data/README.md`; `RESPONSIBLE_AI.md` sections "Data scope" and "Known limitations" | A model for Uzbekistan needs local products, stores, prices, promotions, holidays, retraining, and a new chronological test. |
| 3 | What did you clean, and why did you not delete every outlier or zero? | I checked schemas, missing values, identifiers, duplicates, types, negative/fractional sales, prices, and date relationships. Zero sales may be real, and high sales may be genuine demand, so they were reviewed rather than automatically deleted. | notebooks `01_data_audit.ipynb` and `02_data_cleaning.ipynb`; `reports/data_audit_summary.csv`; `reports/cleaning_summary.csv` | Domain rules are safer than blindly removing statistically unusual values. |
| 4 | How did you prevent leakage? | I used chronological train, validation, and test periods. Every sales lag and rolling feature uses past weeks only, the target is excluded from features, and Random Forest was selected before opening the final test target. | `reports/leakage_checks.csv` all PASS rows; `reports/final_model_checks.csv` rows `Random Forest selected before test`, `final fit contains train and validation only`, and `chronological final fit before test` | Future retraining must preserve the same time-order rules. |
| 5 | What baseline did you use, and why Random Forest? | The main baseline predicts the mean of the previous four weeks. On validation it had MAE 4.5172; Linear Regression had 4.7662; Random Forest had 4.2698. Random Forest was selected because it produced the lowest validation MAE and can represent nonlinear interactions. | `reports/baseline_metrics_validation.csv`; `reports/linear_regression_metrics_validation.csv`; `reports/random_forest_metrics_validation.csv` | This was a controlled course-scale experiment, not an exhaustive search of every forecasting method. |
| 6 | Why only 40 trees and these Random Forest settings? | The configuration was deliberately limited for reproducible CPU training on millions of weekly rows. Forty trees, depth and sampling limits reduced time and memory while still beating the validation baseline. | notebook `10_random_forest_mlflow.ipynb`; `reports/mlflow_random_forest_run.csv`; `reports/random_forest_metrics_validation.csv`; `reports/final_model_checks.csv` row `fitted tree count` | More trees might improve stability slightly, but must be tested on validation rather than assumed to be better. |
| 7 | Where is the final unseen-data evaluation, and why is it valid? | After selecting Random Forest on validation, I fitted the fixed configuration on train plus validation and evaluated it on 57,480 later test rows. Test MAE is 4.4179 versus 4.7874 for the baseline, a 7.72 percent improvement. | `reports/final_test_metrics.csv`; `reports/final_model_checks.csv` | The test set was opened once and must not be reused for further tuning. |
| 8 | Why use MAE, and what does 4.4179 mean? | MAE is the average absolute difference between predicted and actual weekly units. A test MAE of 4.4179 means the forecast is off by about 4.42 units per product-store-week on average. | `reports/final_test_metrics.csv`; `reports/error_analysis_summary.csv` | MAE hides slice differences, so RMSE and error analysis are also reported. |
| 9 | Show one concrete failure or edge case. | For actual weekly sales of 21 or more units, MAE rises to 12.33 and mean error is -5.10, meaning average underprediction of 5.10 units. WI_2 is the weakest store slice with MAE 6.50. | `reports/error_analysis_demand_bands.csv` row `high_21_plus`; `reports/error_analysis_stores.csv` row `WI_2` | High-volume forecasts should be reviewed by a planner rather than used automatically. |
| 10 | What data does the model use for one prediction? | It uses 20 features: past sales lags, rolling means and standard deviations, zero-demand history, current and past price information, calendar fields, event counts, and SNAP counts. | `src/inference.py` feature list; `reports/feature_definitions.csv`; columns in `rehearsal/demo_sales_features.xlsx` | The user must supply features from information available before the forecast week; the model does not read future sales. |
| 11 | Show the end-to-end demo route. | Start the Flask app, open the showcase, upload the prepared Excel file, and show the returned forecast table. The Colab fallback runs `demo.ipynb`, loads the committed model, validates the input, and prints a prediction. | `rehearsal/start_rehearsal.ps1`; `rehearsal/showcase/index.html`; `rehearsal/demo_sales_features.xlsx`; `demo.ipynb`; `API.md` | The local UI requires the server; keep the notebook ready as a separate reproducible route. |
| 12 | How can another person reproduce the demo? | Clone the repository, install `requirements.txt`, and run all cells in `demo.ipynb`. The recorded clean-clone check reproduced 10.4885 without raw data or a local MLflow database. | `REPRODUCIBILITY.md` sections "Fast assessment path" and "Verified clean-clone result" | Reproducing full training additionally requires obtaining the M5 source files. |
| 13 | What is the main Responsible AI risk? | Underprediction can cause understocking, especially for high-demand items. The safeguards are human review, monitoring by demand slice, clear display of limitations, and retraining before use on another retailer or country. | `RESPONSIBLE_AI.md` sections "Known limitations" and "Possible harm and safeguards"; `reports/error_analysis_demand_bands.csv` | The output must not be treated as an automatic purchase order. |
| 14 | What would you improve next? | I would add local promotion and stock-availability data so the model can understand sudden demand changes and distinguish demand from stockouts. | `submission/DEFENSE_SCRIPT.md` closing answer "What would you improve next?"; `RESPONSIBLE_AI.md` section "Known limitations" | The new data must first be audited, joined without leakage, and evaluated on a new chronological split. |

## Required live rehearsal record

- Question selected at random: Pending student rehearsal
- Answer duration: Pending; must be no more than 60 seconds
- Exact evidence opened: Pending
- What was weak or uncertain: Pending
- Revision after rehearsal: Pending
