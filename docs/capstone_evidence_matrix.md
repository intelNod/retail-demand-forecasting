# Capstone Evidence Matrix

`Exact evidence location` is a repository path plus a specific section, row, or output. Status is based only on evidence that currently exists.

| Criterion | What I claim | Exact evidence location | Why this is proof | Status | Gap / risk | Next action |
|---|---|---|---|---|---|---|
| 1. Problem Definition and Project Alignment - 10 pts / min 6 | The project forecasts next-week FOODS demand for a retail planner and follows the documented RET-01 scope. | `PROJECT_BRIEF.md` sections 2-3, 5, 9, and 14; `README.md` opening and "Model choice and limitations" | These sections identify the user, business decision, regression target, scope, success criteria, and implemented model route. | YELLOW | `PROJECT_BRIEF.md` section 17 still says mentor approval is pending. | Student: obtain the real mentor decision and update only that field before submission. |
| 2. Data and Preprocessing Pipeline - 15 pts / min 9 | The M5 source was audited and cleaned, daily sales were transformed into leakage-safe weekly features, and the split is chronological. | `data/README.md`; notebooks `01_data_audit.ipynb` through `07_feature_engineering.ipynb`; `reports/data_audit_summary.csv`; `reports/cleaning_summary.csv`; `reports/split_summary.csv`; all eight PASS rows in `reports/leakage_checks.csv` | The notebooks contain the executable workflow; the reports record actual audit/cleaning counts, 3,003,902 train rows, 57,480 validation rows, 57,480 test rows, and explicit leakage checks. | GREEN | Full-data recreation is heavier than the focused demo. | Keep the focused inference demo as the defense route and use the numbered notebooks when asked about preparation. |
| 3. Modeling and Experiments - 20 pts / min 12 | A baseline, Linear Regression, and Random Forest were compared; Random Forest was selected on validation MAE and tracked with MLflow. | notebooks `08_baseline.ipynb` to `10_random_forest_mlflow.ipynb`; `reports/baseline_metrics_validation.csv`; `reports/linear_regression_metrics_validation.csv`; `reports/random_forest_metrics_validation.csv`; `reports/mlflow_random_forest_run.csv` | The validation report contains baseline MAE 4.5172, Linear Regression MAE 4.7662, and Random Forest MAE 4.2698, supporting selection before the test set was opened. | GREEN | The compact Random Forest configuration is a course-scope trade-off, not an exhaustive hyperparameter search. | Explain that 40 trees and controlled sampling were chosen for reproducible CPU runtime and validated performance. |
| 4. Evaluation and Error Analysis - 15 pts / min 9 | The frozen Random Forest was evaluated once on unseen dates and compared with the baseline; difficult slices were analyzed. | `reports/final_test_metrics.csv`; `reports/final_model_checks.csv` row `Random Forest selected before test`; notebook `12_final_error_analysis.ipynb`; `reports/error_analysis_summary.csv`; `reports/error_analysis_demand_bands.csv` row `high_21_plus`; `reports/error_analysis_stores.csv` row `WI_2` | The test report records 57,480 rows, model MAE 4.4179 versus baseline 4.7874; the slice reports show high-demand MAE 12.33 and identify weaker stores/departments. | GREEN | High-volume demand is systematically harder and is underpredicted. | Present human review as the safeguard and do not describe the average MAE as equal performance for every product. |
| 5. End-to-End Implementation and Project Delivery - 20 pts / min 12 | A saved model accepts validated features and produces predictions through a notebook, reusable Python function, REST API, browser UI, and Excel batch route. | `demo.ipynb` all cells; `models/final_model/model.skops`; `src/inference.py`; `src/validation.py`; `src/app.py`; `API.md` sections "Excel batch prediction", "Prediction request", and "Error behavior"; tests in `tests/` | The same committed artifact is loaded for inference, input errors are rejected, and the routes are covered by automated tests. | GREEN | The local Flask UI requires the server to be started before the page opens. | Before defense, run `rehearsal/start_rehearsal.ps1`, upload the workbook, and keep `demo.ipynb` open as fallback. |
| 6. Documentation and Reproducibility - 10 pts / min 6 | Another person can install dependencies and reproduce the focused demo from the repository. | `README.md` sections "Run the audit", "Safety and reproducibility", and "Reusable prediction function"; `REPRODUCIBILITY.md` sections "Fast assessment path" and "Verified clean-clone result"; `requirements.txt` | The clean-clone record states that the notebook ran without hidden raw data and reproduced prediction 10.4885; commands and dependencies are documented. | GREEN | Full training needs the external M5 files and more resources than the focused demo. | Clearly distinguish the quick inference reproduction path from the full numbered training workflow. |
| 7. Responsible AI and Limitations - 5 pts / min 3 | The forecast is decision support with human review, and geographic, temporal, error, privacy, and misuse limits are documented. | `RESPONSIBLE_AI.md` sections "Data scope", "Known limitations", "Possible harm and safeguards", and "Transparency"; `reports/error_analysis_demand_bands.csv` | The safeguards are project-specific: high-demand underprediction, old US retailer data, aggregate non-personal data, monitoring, and no autonomous ordering. | GREEN | Results cannot be assumed to transfer to Uzbekistan or a different retailer. | Retrain and revalidate on local retailer data before any local operational use. |
| 8. Presentation, Demo, and Q&A - 5 pts / min 3 | English slides, a five-minute route, a working demonstration path, and evidence-linked Q&A preparation exist. | `submission/defense_presentation_final_en.pptx`; `docs/defense_pitch_outline.md`; `docs/defense_question_bank.md`; `rehearsal/README_RU.md`; `rehearsal/demo_sales_features.xlsx` | These artifacts provide the visual story, timed route, real demo input, and direct links from likely questions to project proof. | YELLOW | The student has not yet recorded a timed five-minute rehearsal or completed the three blind locator tests. | Student: perform the rehearsal and locator tests, then replace pending fields with real results. |

## Essential requirements

| Requirement | Status | Exact evidence / blocker |
|---|---|---|
| At least 60/100 overall | YELLOW | Only mentors can assign the official score; the rows above map evidence to every criterion. |
| Trained or fine-tuned ML model | GREEN | `models/final_model/model.skops`; `reports/final_model_checks.csv` rows `fitted tree count` and `final model reload reproduces test MAE`. |
| Evaluation on unseen data with task-appropriate metrics | GREEN | `reports/final_test_metrics.csv`; `reports/final_model_checks.csv` rows proving selection before test and chronological fit before test. |
| Working end-to-end demo | GREEN | `demo.ipynb`; `src/app.py`; `rehearsal/demo_sales_features.xlsx`; automated tests in `tests/`. |
| Clear reproduction instructions | GREEN | `REPRODUCIBILITY.md` and `README.md` sections described above. |
| Final defense attendance and project Q&A | YELLOW | Attendance and live answers can only be completed by the student at the defense. |

## Solo Show-Me-Where tests

Perform these from the repository root without search and without relying on memory.

| # | High-risk claim | Written exact location | Result | Missing context | Next action |
|---|---|---|---|---|---|
| 1 | The final score comes from unseen dates and was not used for model selection. | Open `reports/final_model_checks.csv`; read the rows `Random Forest selected before test`, `final fit contains train and validation only`, `test split only`, and `chronological final fit before test`; then open `reports/final_test_metrics.csv`. | PENDING | Requires student blind-locator rehearsal. | Mark PASS only after reaching and explaining the proof without search. |
| 2 | The model has a concrete high-demand weakness. | Open `reports/error_analysis_demand_bands.csv`; read the `high_21_plus` row, especially `mae=12.3305` and `mean_error=-5.1011`. | PENDING | Requires student blind-locator rehearsal. | Mark PASS only after explaining that the negative error means average underprediction. |
| 3 | Another person can run a real prediction without the raw M5 files. | Open `REPRODUCIBILITY.md` section "Fast assessment path", then run all cells in `demo.ipynb` and confirm prediction `10.4885` plus invalid-input rejection. | PENDING | Requires student/Colab execution during rehearsal. | Record the actual environment and PASS/FAIL result. |

## Readiness conclusion

Current evidence readiness is **YELLOW**. The technical model and demo evidence are present, but mentor approval, the official LMS deadline, the timed rehearsal, the three locator tests, and final defense attendance remain real student/mentor actions.
